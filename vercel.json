{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "buildCommand": "npm install && prisma generate --no-engine && prisma generate && npm run build",
  "installCommand": "npm ci",
  "env": {
    "PRISMA_CLI_BINARY_TARGETS": "rhel-openssl-3.0.x,rhel-openssl-1.0.x,native"
  }
}